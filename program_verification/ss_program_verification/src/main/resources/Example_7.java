//Empty if body
public class Example {

    public void swap(int x, int y) {
        int tmp = x;
        //x; tmp
        if (tmp > y) {
            int x = y;
        } else {
            int tmp = y;
        }
        int y = tmp;
    }
}
